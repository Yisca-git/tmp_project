import { ModelModel } from "./model.model";

export class FinalModelModel {
    sort(arg0: () => number) {
      throw new Error('Method not implemented.');
    }
    items!: ModelModel[];
    totalCaunt!: number;
    hasNext!:boolean;
    hasPrev!: boolean;
}